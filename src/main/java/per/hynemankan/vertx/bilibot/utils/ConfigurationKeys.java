package per.hynemankan.vertx.bilibot.utils;

public enum ConfigurationKeys {
  /**
   * ping测回复
   */
  PING_RESPONSE,
  /**
   * Redis配置
   */
  CONNECT_STRING,
  PASSWORD,
  MAX_POOL_SIZE,
  MAX_WAITING_HANDLERS,
}
